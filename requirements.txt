# ETL Framework — Python Dependencies
# Install with: pip install -r requirements.txt

# Core
pyspark==3.5.0
delta-spark==3.0.0

# Databricks SDK (for local testing without cluster)
databricks-sdk>=0.20.0

# Airflow (for DAG development/testing)
apache-airflow==2.8.0
apache-airflow-providers-databricks>=4.3.0

# Testing
pytest==7.4.0
pytest-mock==3.12.0

# Utilities
python-dateutil>=2.8.2
